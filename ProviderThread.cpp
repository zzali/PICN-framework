//#include "ProviderThread.h"

//ProviderThread::ProviderThread(QObject *parent) :
//    QThread(parent)
//{
//}

//void ProviderThread::init(QTcpSocket *socket)
//{
////    this->socket = socket;
////    socket->moveToThread(this);
////    hProvider = new HTTPProvider();
////    hProvider->moveToThread(this);
////    connect(this, SIGNAL(finished()), this, SLOT(freeMemory()));
////    accessManager = new QNetworkAccessManager();
////    accessManager->moveToThread(this);
//}

//void ProviderThread::run()
//{
//}

//void ProviderThread::freeMemory()
//{
//}
